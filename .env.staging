# WordPress Staging Configuration
WP_BASE_URL=https://staging.your-site.com

# Optional: Override GitHub secrets with environment variables
# WP_API_SECRET=your-staging-api-secret-here
# WP_BASIC_AUTH_USER=admin
# WP_BASIC_AUTH_PASS=password

# Import Configuration (can be overridden by workflow inputs)
IMPORT_MODE=sync
PRODUCT_LIMIT=50
USE_ENHANCED_FILES=true
