name: Daily ESP Scraper

on:
  schedule:
    - cron: "0 3 * * *"  # Every day at 3AM UTC
  workflow_dispatch:      # Manual trigger

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    env:
      PRODUCTS_URL: ${{ secrets.PRODUCTS_URL }}
      SEARCH_API_URL: ${{ secrets.SEARCH_API_URL }}
      GOTO_PAGE_API_URL: ${{ secrets.GOTO_PAGE_API_URL }}
      ESP_USERNAME: ${{ secrets.ESP_USERNAME }}
      ESP_PASSWORD: ${{ secrets.ESP_PASSWORD }}
      PYTHONPATH: .

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 🔧 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 🚀 Run ESP scraper
        run: |
          python -m espscraper --limit 10 --headless

      - name: 📦 Checkout artifacts branch
        uses: actions/checkout@v4
        with:
          ref: artifacts
          path: artifacts-branch

      - name: 📝 Copy new artifacts
        run: |
          cp -r espscraper/data/*.json* artifacts-branch/ || true
          cp -r log/scraper.log artifacts-branch/ || true
          mkdir -p artifacts-branch/tmp
          cp -r tmp/* artifacts-branch/tmp/ || true

      - name: 🚀 Commit and push artifacts
        run: |
          cd artifacts-branch
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config --global url."https://${GITHUB_TOKEN}:x-oauth-basic@github.com/".insteadOf "https://github.com/"
          git add .
          git commit -m "Update artifacts from workflow run $(date)" || echo "No changes to commit"
          git push origin artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
